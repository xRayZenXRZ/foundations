# foundations

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.4.2. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.


# Questions d'observation de l'exercice 2 :

1. Retourne un tableau avec tous les éléments remplissant les conditions choisi, parcourant 1 par 1 dans chaque élément du tableau ciblée.
2. Joue le rôle du filter choisi cette-à-dire elle filtre le tableau pour ne conserver que les membres disponibles 
3. Ces fonctions ne produisant pas une nouvelle valuer, elles font office uniquement de filtre en dehors de getDisplayName qui retourne une nouvelle string formatter avec les propriétés choisi.

# Questions d'observation de l'exercice 3 :

1. Aucune fonction de l'exercice 3 calculent une valeur
2. / 
3. Non

# Question d'observation de l'exercice 4 : 

1. le rajout du partners mais également updated aurore represente une équipe entière
2. name, base, title, membercount et l'id, partners et l'id obtiennet de nouvelles references.
3. car cela reste le même état tout le temps.
4. il n'y aura pas d'érreur important en dehors d'une erreur de conceptuelle et logique.

# Question d'observation de l'exercice 5 : 

5.2 -> "ReferencError : Cannot acces 'sucessState' before intialisation."

1. elle decrit le résultat à produire selon l'état.
2. on restreint le type par une condition ainsi que leur output , je crois c'est ce qu'on a fais ?
3. la description d'un mission d'etat non défini ira dans le condition default.

# Question d'observation de l'exercice 6 :

6.1
1. l'interface de collection par default
2. on garantit de choisir le bon identifiant 
3. Si on ne retrouve pas le team notre fonction depart renvoie undefined .


